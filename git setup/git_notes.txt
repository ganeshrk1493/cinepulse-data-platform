git config --global user.name "ganeshrk1493"
git config --global user.email "ganeshrk1493@gmail.com"
git config --global init.defaultBranch main

create github repo in github.com (same user and mail as above)
https://github.com/ganeshrk1493/cinepulse-data-platform.git



Initialize local git :

cd G:\Cine-pulse

# 1. Initialize local git tracking
git init

# 2. Stage all project files (ignoring files in .gitignore)
git add .

# 3. Commit the baseline code
git commit -m "feat: initial commit with reddit fetcher, project specs, and terraform foundation"

# 4. Link your local project to the GitHub remote repository
git remote add origin https://github.com/ganeshrk1493/cinepulse-data-platform.git

# 5. Push to GitHub
git push -u origin main

